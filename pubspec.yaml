name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1

  # Firebase dependencies
  firebase_core: ^3.13.1        # Latest version of firebase_core
  firebase_auth: ^5.5.4
  cloud_firestore: ^5.6.8       # Compatible with firebase_core ^3.13.1
  firebase_storage: ^12.4.7     # Latest version of firebase_storage
  image_picker: ^0.8.5+3
  image: ^3.0.1
  intl: ^0.19.0
  cupertino_icons: ^1.0.8
  lottie: ^2.7.0
  supabase_flutter: ^2.0.0
  provider: ^6.1.1
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true   # ✅ Enables localization code generation

  assets:
    - assets/logo.png
    - assets/images/
    - assets/student_logo.png
    - assets/student2_logo.png
    - assets/tutor_logo.png
    - assets/kayes.jpg
    - assets/tabassum.jpg
    - assets/swapon.jpg
    - assets/akash.jpg
